export const metadata = {
  title: "Activity Log | Disibin Dashboard",
  description: "Audit system activities, staff actions, and operational logs.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
