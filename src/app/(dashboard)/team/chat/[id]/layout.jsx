export const metadata = {
  title: "Team Chat Room | Disibin Dashboard",
  description: "Staff team discussion and project collaboration chat.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
