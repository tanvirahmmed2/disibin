export const metadata = {
  title: "Report Details | Disibin Dashboard",
  description: "View submitted client report details.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
