export const metadata = {
  title: "Client Reports | Disibin Dashboard",
  description: "Manage client reports and audit submissions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
