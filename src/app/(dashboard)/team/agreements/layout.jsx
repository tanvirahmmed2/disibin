export const metadata = {
  title: "Agreements Management | Disibin Dashboard",
  description: "Manage customer service contracts, legal agreements, and documents.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
