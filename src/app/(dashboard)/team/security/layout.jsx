export const metadata = {
  title: "Staff Security Settings | Disibin Dashboard",
  description: "Configure staff 2FA, password, and security preferences.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
