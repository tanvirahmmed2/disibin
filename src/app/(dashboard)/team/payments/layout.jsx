export const metadata = {
  title: "Payments Ledger | Disibin Dashboard",
  description: "Financial transaction history, dues, and invoice tracking.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
