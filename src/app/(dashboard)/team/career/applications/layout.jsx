export const metadata = {
  title: "Job Applications | Disibin Dashboard",
  description: "Review candidate applications and talent submissions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
