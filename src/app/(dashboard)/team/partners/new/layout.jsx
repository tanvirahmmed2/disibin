export const metadata = {
  title: "Add New Partner | Disibin Dashboard",
  description: "Add an enterprise partner logo and details.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
