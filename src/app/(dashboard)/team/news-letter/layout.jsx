export const metadata = {
  title: "Newsletter Subscribers | Disibin Dashboard",
  description: "View and export newsletter subscriber lists.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
