export const metadata = {
  title: "FAQs Management | Disibin Dashboard",
  description: "Manage public help center articles and FAQ items.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
