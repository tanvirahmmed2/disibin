export const metadata = {
  title: "System Settings | Disibin Dashboard",
  description: "Configure platform settings and system preferences.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
