export const metadata = {
  title: "User Login Logs | Disibin Dashboard",
  description: "Audit log of user login sessions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
