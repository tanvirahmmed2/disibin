export const metadata = {
  title: "Refund Policy Management | Disibin Dashboard",
  description: "Manage refund policy provisions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
