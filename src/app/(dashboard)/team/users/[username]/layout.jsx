export const metadata = {
  title: "User Details | Disibin Dashboard",
  description: "View customer profile, projects, and history.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
