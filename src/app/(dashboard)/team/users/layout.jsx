export const metadata = {
  title: "Registered Users | Disibin Dashboard",
  description: "Manage customer accounts, roles, and status.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
