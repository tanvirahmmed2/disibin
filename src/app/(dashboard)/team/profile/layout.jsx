export const metadata = {
  title: "Staff Profile | Disibin Dashboard",
  description: "Manage personal staff profile and account credentials.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
