export const metadata = {
  title: "Client Leads | Disibin Dashboard",
  description: "Manage individual client leads and customer inquiries.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
