export const metadata = {
  title: "Leads Overview | Disibin Dashboard",
  description: "Overview of captured sales leads and business inquiries.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
