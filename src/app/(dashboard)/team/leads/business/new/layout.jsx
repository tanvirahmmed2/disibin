export const metadata = {
  title: "New Business Lead | Disibin Dashboard",
  description: "Add a new enterprise lead record.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
