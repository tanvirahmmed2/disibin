export const metadata = {
  title: "Team Login Logs | Disibin Dashboard",
  description: "Audit log of staff team login sessions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
