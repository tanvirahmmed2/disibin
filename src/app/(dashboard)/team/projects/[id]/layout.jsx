export const metadata = {
  title: "Project Details | Disibin Dashboard",
  description: "Staff project management, task tracking, and customer billing.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
