export const metadata = {
  title: "Projects Management | Disibin Dashboard",
  description: "Manage client project pipelines, statuses, and workspaces.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
