export const metadata = {
  title: "Add New Product | Disibin Dashboard",
  description: "Create a new software product or service.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
