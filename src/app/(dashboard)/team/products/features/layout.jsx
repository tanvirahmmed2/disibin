export const metadata = {
  title: "Product Features | Disibin Dashboard",
  description: "Manage product features and system specs.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
