export const metadata = {
  title: "Add Team Member | Disibin Dashboard",
  description: "Invite or create a new staff team member account.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
