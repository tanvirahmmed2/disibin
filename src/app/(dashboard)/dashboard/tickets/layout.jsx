export const metadata = {
    title: 'Ticket Management | Dashboard',
    description: 'View and manage active support tickets and client requests.',
}

export default function TicketsLayout({ children }) {
    return <>{children}</>
}
