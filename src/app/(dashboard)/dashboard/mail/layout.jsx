export const metadata = {
    title: 'Internal Mail | Dashboard',
    description: 'Internal communication system for Disibin management and staff.',
}

export default function MailLayout({ children }) {
    return <>{children}</>
}
