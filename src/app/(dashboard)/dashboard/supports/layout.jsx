import React from 'react'

export const metadata={
    title:'Supports | Disibin',
    description:'Supports Disibin page'
}

const SupportsLayout = ({children}) => {
  return (
    <div className='w-full'>
      {children}
    </div>
  )
}

export default SupportsLayout
