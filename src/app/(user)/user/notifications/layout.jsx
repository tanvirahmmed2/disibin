export const metadata = {
  title: "Notifications | User Panel",
  description: "View your activity notifications, project updates, and system alerts.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
