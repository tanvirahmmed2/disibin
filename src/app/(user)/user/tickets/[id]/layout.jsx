export const metadata = {
  title: "Support Ticket Details | User Panel",
  description: "Track support ticket status, messages, and resolutions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
