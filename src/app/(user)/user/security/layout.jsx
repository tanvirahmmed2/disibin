export const metadata = {
  title: "Security Settings | User Panel",
  description: "Manage account password, two-factor authentication, and active sessions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
