export const metadata = {
  title: "My Projects | User Panel",
  description: "Manage your active projects, workspaces, and system developments.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
