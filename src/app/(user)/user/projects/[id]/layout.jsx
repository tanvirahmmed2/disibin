export const metadata = {
  title: "Project Workspace | User Panel",
  description: "View project details, updates, milestones, and project discussions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
