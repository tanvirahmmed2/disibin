export const metadata = {
    title: 'Subscription Details | User Center',
    description: 'Manage your active subscriptions and billing information.',
}

export default function SubscriptionLayout({ children }) {
    return <>{children}</>
}
