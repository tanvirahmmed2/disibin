export const metadata = {
  title: "Purchases & Receipts | User Panel",
  description: "Track your software purchases, service orders, and download receipts.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
