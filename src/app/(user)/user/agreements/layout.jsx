export const metadata = {
  title: "My Agreements | User Panel",
  description: "Review and sign software agreements and service contracts.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
