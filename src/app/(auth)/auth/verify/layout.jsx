import React from 'react'

export const metadata={
    title:"Verify Account | Disibin",
    description:"Verify and access your account. Disibin"
}

const layout = ({children}) => {
  return (
    <>{children}</>
  )
}

export default layout