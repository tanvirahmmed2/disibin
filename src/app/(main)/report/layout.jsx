export const metadata = {
  title: "Report an Issue | Disibin",
  description: "Submit a system report, security vulnerability, or technical feedback to Disibin.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
