export const metadata = {
  title: "Reset Password | Disibin Security",
  description: "Securely enter a new password for your Disibin account.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
