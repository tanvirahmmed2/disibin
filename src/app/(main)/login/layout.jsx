export const metadata = {
  title: "Sign In | Disibin Portal",
  description: "Sign in to your Disibin user or staff account to manage support tickets, developer projects, and system settings.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
